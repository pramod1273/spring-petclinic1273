apiVersion: v1
kind: Service
metadata:
  name: app-lb-service
  labels:
    app: test

spec:
  type: LoadBalancer
  selector:
     app: jar

  ports:
    - protocol: TCP
      port: 1055
      targetPort: 8080
